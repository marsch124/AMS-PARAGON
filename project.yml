# XcodeGen spec. Run `xcodegen generate` and open Paragon.xcodeproj.
# Any change here makes CI regenerate the committed project (needed after adding source files).
name: Paragon
options:
  bundleIdPrefix: com.schabbauer
  deploymentTarget:
    macOS: "14.0"
    iOS: "17.0"
  createIntermediateGroups: true
  generateEmptyDirectories: false

packages:
  ParagonCore:
    # The package lives in its own folder: Xcode cannot reference a local
    # package that sits in the same directory as the .xcodeproj.
    path: Core

targets:
  Paragon:
    type: application
    supportedDestinations: [macOS, iOS]
    sources:
      # Synchronized folder (Xcode 16+): new source files are picked up without
      # regenerating the project, so the committed project rarely changes.
      - path: App/Paragon
        type: syncedFolder
      # The built-in manual (Help window on the Mac, Settings › Help on iOS).
      - path: Docs
        buildPhase: resources
    dependencies:
      - package: ParagonCore
        product: ParagonCore
      - target: ParagonShare
        platformFilter: iOS
      # Both platforms since build 178. It was iPhone only in 174, and he then said he uses
      # the widget on the Mac only: the PARAGON widget the Mac could offer him came from the
      # phone over iPhone Mirroring, which Apple does not provide in the EU, so it could only
      # ever draw a blank box.
      - target: ParagonWidgets
    info:
      path: App/Config/Paragon/Info.plist
      properties:
        CFBundleDisplayName: PARAGON
        CFBundleName: PARAGON
        UTExportedTypeDeclarations:
          - UTTypeIdentifier: com.schabbauer.amspara.task
            UTTypeDescription: PARAGON task
            UTTypeConformsTo: [public.data]
        # Without this key macOS and iOS ignore the AppIcon set in the asset catalog.
        CFBundleIconName: AppIcon
        LSApplicationCategoryType: public.app-category.productivity
        CFBundleURLTypes:
          - CFBundleURLName: com.schabbauer.amspara
            CFBundleURLSchemes: [amspara]
        NSCalendarsFullAccessUsageDescription: PARAGON shows calendar events for the day next to your tasks in Today and in daily notes.
        NSCalendarsUsageDescription: PARAGON shows calendar events for the day next to your tasks in Today and in daily notes.
        NSRemindersFullAccessUsageDescription: PARAGON mirrors the tasks in your PARA notes to Apple Reminders and back.
        NSRemindersUsageDescription: PARAGON mirrors the tasks in your PARA notes to Apple Reminders and back.
        NSHumanReadableCopyright: ""
        UILaunchScreen: {}
        # Apple rejects an upload without these: iPad multitasking needs all four.
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportsDocumentBrowser: true
        LSSupportsOpeningDocumentsInPlace: true
    entitlements:
      path: App/Config/Paragon/Paragon.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.files.user-selected.read-write: true
        com.apple.security.files.bookmarks.app-scope: true
        com.apple.security.personal-information.calendars: true
        com.apple.security.application-groups: [group.com.schabbauer.amspara]
    settings:
      base:
        # The built app is PARAGON.app, not Paragon.app: on the Mac, Finder and the Dock
        # show the bundle's file name, so CFBundleDisplayName alone would leave the old
        # name on screen. The target, the folder and the bundle id are untouched.
        PRODUCT_NAME: PARAGON
        # Pinned, not derived from the target name: this is the app's identity to Apple and
        # changing it would make PARAGON a different app — new TestFlight, fresh install.
        PRODUCT_BUNDLE_IDENTIFIER: com.schabbauer.AMSPara
        SWIFT_VERSION: "5.0"
        MARKETING_VERSION: 0.1.0
        CURRENT_PROJECT_VERSION: 1
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: D24ENP83QQ
        CODE_SIGN_ENTITLEMENTS[sdk=macosx*]: App/Config/Paragon/Paragon-macOS.entitlements
        GENERATE_INFOPLIST_FILE: false
        ENABLE_HARDENED_RUNTIME: true
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

  ParagonShare:
    type: app-extension
    platform: iOS
    sources:
      - path: App/ParagonShare
        type: syncedFolder
    dependencies:
      - package: ParagonCore
        product: ParagonCore
    info:
      path: App/Config/ParagonShare/Info.plist
      properties:
        CFBundleDisplayName: PARAGON
        NSExtension:
          NSExtensionPointIdentifier: com.apple.share-services
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).ShareViewController
          NSExtensionAttributes:
            NSExtensionActivationRule:
              NSExtensionActivationSupportsText: true
              NSExtensionActivationSupportsWebURLWithMaxCount: 1
              NSExtensionActivationSupportsWebPageWithMaxCount: 1
    entitlements:
      path: App/Config/ParagonShare/ParagonShare.entitlements
      properties:
        com.apple.security.application-groups: [group.com.schabbauer.amspara]
    settings:
      base:
        SWIFT_VERSION: "5.0"
        MARKETING_VERSION: 0.1.0
        CURRENT_PROJECT_VERSION: 1
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: D24ENP83QQ
        GENERATE_INFOPLIST_FILE: false
        PRODUCT_BUNDLE_IDENTIFIER: com.schabbauer.AMSPara.Share
        SKIP_INSTALL: true

  # The widget (build 174 on the iPhone, build 178 on the Mac as well). It reads one small
  # file out of the App Group container that the app writes on every reload; it never opens
  # the vault, which is a security-scoped bookmark only the app can resolve.
  ParagonWidgets:
    type: app-extension
    supportedDestinations: [iOS, macOS]
    sources:
      - path: App/ParagonWidgets
        type: syncedFolder
    dependencies:
      - package: ParagonCore
        product: ParagonCore
    info:
      path: App/Config/ParagonWidgets/Info.plist
      properties:
        CFBundleDisplayName: PARAGON
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    entitlements:
      path: App/Config/ParagonWidgets/ParagonWidgets.entitlements
      properties:
        com.apple.security.application-groups: [group.com.schabbauer.amspara]
    settings:
      base:
        SWIFT_VERSION: "5.0"
        MARKETING_VERSION: 0.1.0
        CURRENT_PROJECT_VERSION: 1
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: D24ENP83QQ
        GENERATE_INFOPLIST_FILE: false
        PRODUCT_BUNDLE_IDENTIFIER: com.schabbauer.AMSPara.Widgets
        SKIP_INSTALL: true
        # The Mac needs its own entitlements: an extension inside a sandboxed Mac app must be
        # sandboxed itself, and the App Group must carry the Team ID in front of it — the
        # plain name is an iOS spelling and the Mac App Store refuses it.
        CODE_SIGN_ENTITLEMENTS[sdk=macosx*]: App/Config/ParagonWidgets/ParagonWidgets-macOS.entitlements

  # The screen tests (build 190). A target of its own and a **scheme of its own**, so the
  # Paragon scheme that TestFlight archives cannot pick any of this up: a Release archive must
  # never contain a test bundle, and the safest way to guarantee that is for the archive scheme
  # not to know it exists.
  ParagonUITests:
    type: bundle.ui-testing
    # Both since build 195: the faults that cost the most (71-74, 85) were the Mac's.
    supportedDestinations: [iOS, macOS]
    sources:
      - path: Tests/ParagonUITests
    dependencies:
      - target: Paragon
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.schabbauer.AMSPara.ScreenTests
        PRODUCT_NAME: ParagonUITests
        SWIFT_VERSION: "5.0"
        GENERATE_INFOPLIST_FILE: true
        TEST_TARGET_NAME: Paragon
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: D24ENP83QQ
        TARGETED_DEVICE_FAMILY: "1,2"

schemes:
  Paragon:
    build:
      targets:
        Paragon: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

  # `xcodebuild -scheme ParagonUITests test` is what CI runs on a simulator.
  ParagonUITests:
    build:
      targets:
        ParagonUITests: [test]
    test:
      config: Debug
      targets:
        - ParagonUITests
